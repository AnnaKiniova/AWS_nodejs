// import { isMainThread } from "worker_threads";

// describe('Test getProductList', () => {
//     it('should return error BAD_REQUEST when path is invalid', async() => {
//         await createEvent() = {

//         }
//     });
